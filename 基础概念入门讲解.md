# STM32 OTA 底层概念入门讲解

> 本文专为回答以下疑问编写：
> 1. 栈顶是什么？SRAM1 是什么？Reset_Handler、Thumb 又是什么？
> 2. 软复位法、RTC 备份寄存器、NVIC_SystemReset 是什么？
> 3. 代码里的 `sp` 是什么？
> 4. "APP 写标志 + Bootloader 读标志跳转"两步到底怎么运作？
>
> 阅读前提：你知道 C 语言基本语法，用过 STM32 的 GPIO/串口，但没接触过 Bootloader/OTA。

---

## 第一部分：先搞懂"程序在芯片里怎么运行"

### 1.1 Flash 和 SRAM 的区别（先打地基）

STM32 芯片里有两种内存，功能完全不同：

| 类型 | 作用 | 类比 | 断电后 |
|------|------|------|--------|
| **Flash** | 存放**程序代码**（你写的 .c 编译后的机器码） | 电脑硬盘 | **不丢失** |
| **SRAM** | 存放**运行时数据**（变量、栈、堆） | 电脑内存条 | **丢失** |

```
┌──────────────── STM32 芯片 ────────────────┐
│                                             │
│  Flash（1MB）       SRAM（192KB）           │
│  ┌──────────┐       ┌──────────────┐       │
│  │ 程序代码  │       │ 变量/栈/堆    │       │
│  │ (只读运行) │       │ (读写运行)    │       │
│  └──────────┘       └──────────────┘       │
│   0x08000000起       0x20000000起           │
└─────────────────────────────────────────────┘
```

- **程序烧到 Flash**：你用 Keil 下载，就是把代码写进 Flash。断电不丢，下次上电直接跑。
- **SRAM 是运行时的工作区**：程序跑起来后，变量、函数调用的临时数据都放 SRAM。断电就没了。

### 1.2 上电后 CPU 从哪里开始执行？

Cortex-M 内核有个铁律：**上电后，CPU 自动去 Flash 的最开头（0x08000000）取两个数据**。

```
Flash 地址        存的内容
0x08000000  ┌─────────────────┐
            │  0x20020000     │ ← 第1个字：栈顶地址（SP）  ← CPU 把它装进 MSP 寄存器
0x08000004  ├─────────────────┤
            │  0x08000189     │ ← 第2个字：Reset_Handler  ← CPU 跳到这里执行
0x08000008  ├─────────────────┤
            │  NMI_Handler    │ ← 第3个字：NMI 中断入口
0x0800000C  ├─────────────────┤
            │  HardFault_Handler│ ← 第4个字：硬件错误入口
            └─────────────────┘
```

这一片连续的数据叫**向量表（Vector Table）**。CPU 上电只关心前两个字：
1. 第 1 个字 → 装进栈指针寄存器（MSP）
2. 第 2 个字 → 当成函数地址，跳过去执行

**这就是为什么 Bootloader 要检查这两个字来判断 APP 是否有效。**

---

## 第二部分：栈顶（SP）是什么？

### 2.1 什么是"栈"？

**栈（Stack）是一块特殊的 SRAM 区域，用来存放函数调用时的临时数据**：
- 函数里的局部变量
- 函数调用时的返回地址
- 中断发生时的现场保护

栈的工作方式像**弹匣**：后进先出（LIFO）。函数嵌套调用时，一层层往里压数据；函数返回时，一层层弹出。

```
SRAM 高地址  ┌──────────────┐  ← 栈底（栈从这里往下生长）
             │              │
             │  函数C的变量  │  ← 最后压入的（栈顶，当前SP指向这里）
             │  函数B的变量  │
             │  函数A的变量  │
SRAM 低地址  └──────────────┘
```

### 2.2 什么是"栈顶"？

**栈顶 = 栈目前用到哪个位置了**。CPU 用一个专用寄存器 **SP（Stack Pointer，栈指针）** 来记录栈顶地址。

- 压栈（push）：SP 减小，数据写入新位置
- 出栈（pop）：SP 增大，数据读出

程序刚启动时，栈是空的，SP 指向**栈的最高地址**（栈底），这个初始值就是**栈顶地址**。

### 2.3 栈顶地址从哪来？

就是向量表的**第 1 个字**！链接器根据你的启动文件和链接脚本，算出栈的大小和位置，把栈顶地址写进 Flash 开头。

```c
// 代码里这行就是在读栈顶地址：
uint32_t sp = pAddr[0];   // pAddr 指向 APP 起始地址，pAddr[0] 就是第1个字
```

**所以 `sp` 这个变量 = APP 启动时装进栈指针的初始值 = 栈所在的位置。**

### 2.4 为什么要检查 sp 是否合法？

如果 APP 没烧录，Flash 里全是 0xFF（擦除状态），那 sp 读出来就是 0xFFFFFFFF。这个地址根本不在 SRAM 范围内，CPU 真的用它做栈顶，一压栈就访问非法地址 → HardFault 死机。

所以 Bootloader 要检查：**sp 必须落在真实存在的 SRAM 范围内**，才说明这里真有个 APP。

---

## 第三部分：SRAM1 和 SRAM2 是什么？

### 3.1 STM32F407ZE 的两块 SRAM

这块芯片的 192KB SRAM **分成两块独立区域**：

```
┌─────────────────────────────────────────┐
│  SRAM1：0x20000000 ~ 0x2001FFFF（128KB）│ ← 主内存，默认放变量和栈
├─────────────────────────────────────────┤
│  SRAM2：0x10000000 ~ 0x1000FFFF（64KB） │ ← 高速RAM，可放栈/堆
└─────────────────────────────────────────┘
```

注意两块地址**不连续**：SRAM1 在 0x2000xxxx，SRAM2 在 0x1000xxxx。

### 3.2 为什么复杂 APP 的栈会在 SRAM2？

- **简单项目**（LED 闪烁）：变量少，栈小，链接器默认把栈放 SRAM1，栈顶 ≈ 0x20020000。
- **复杂项目**（FreeRTOS + 蓝牙 + WiFi + OLED）：SRAM1 被各种全局变量、FreeRTOS 任务堆栈塞满，链接器只能把主栈分配到 SRAM2，栈顶 ≈ 0x10010000。

### 3.3 这导致的 Bug

原来 Bootloader 只检查 SRAM1：

```c
// 错误写法：只认 SRAM1
if ((sp >= 0x20000000) && (sp < 0x20020000)) {
    sp_valid = 1;   // 只在这里才算合法
}
```

复杂 APP 的 sp = 0x10010000（在 SRAM2），**不在这个范围**，被判"非法" → Bootloader 认为 APP 无效 → 进入 else 死循环。

修复就是**两个范围都认**：

```c
if ((sp >= 0x20000000) && (sp < 0x20020000))      sp_valid = 1; // SRAM1
else if ((sp >= 0x10000000) && (sp < 0x10010000)) sp_valid = 1; // SRAM2
```

---

## 第四部分：Reset_Handler 是什么？

### 4.1 Reset_Handler = 程序的"真正入口"

C 程序的 `main()` 不是 CPU 执行的第一个函数。上电后真正的执行顺序是：

```
CPU上电
  │
  │ 从 0x08000000 取 sp，从 0x08000004 取 Reset_Handler 地址
  ▼
Reset_Handler（启动文件 startup_stm32f40_41xxx.s 里写好的）
  │
  │ 1. 把 .data 段从 Flash 拷到 SRAM（初始化全局变量）
  │ 2. 把 .bss 段清零（未初始化的全局变量）
  │ 3. 调用 SystemInit() 配置时钟
  │ 4. 调用 __main
  ▼
main()  ← 你写的代码终于开始执行
```

**Reset_Handler 是启动文件里用汇编写的一段代码**，负责"铺地毯"——把运行环境准备好，最后才跳到你的 `main()`。

### 4.2 为什么 Bootloader 要检查它？

向量表第 2 个字就是 Reset_Handler 的地址。Bootloader 检查它：

```c
uint32_t reset_handler = pAddr[1];  // 读第2个字

// 检查1：必须在 Flash 范围内（说明这是个真函数地址，不是0xFFFFFFFF）
if ((reset_handler >= 0x08040000) && (reset_handler < 0x08100000)) {
    // 检查2：Thumb 位（见下一节）
    if (reset_handler & 1) return 1;  // 有效
}
```

如果 APP 没烧，这个字是 0xFFFFFFFF，不在 Flash 范围 → 判定无效。

---

## 第五部分：Thumb 位是什么？

### 5.1 背景：ARM 的两套指令集

ARM 处理器历史上有两套指令集：

| 指令集 | 指令长度 | 特点 |
|--------|----------|------|
| ARM 指令 | 32 位（4字节） | 老的，代码体积大 |
| **Thumb 指令** | 16/32 位混合 | 新的，代码紧凑 |

**Cortex-M 内核（包括 STM32F407）只支持 Thumb 指令集**，不支持 ARM 指令集。

### 5.2 Thumb 位的作用

为了让 CPU 知道"这个地址里的代码是 Thumb 指令还是 ARM 指令"，ARM 规定：

> **所有函数地址的最低位 (bit0) 用来标记指令集：1 = Thumb，0 = ARM。**

注意：这个最低位**不是真实地址的一部分**，CPU 跳转时会自动忽略它。例如：
- 函数真实地址 = 0x08000188
- 存在向量表里 = 0x080001**89**（最低位置1）

### 5.3 为什么要检查 `reset_handler & 1`？

```c
if (reset_handler & 1) return 1;  // 最低位是1，说明是Thumb指令，合法
```

- 合法 APP：Reset_Handler 是 Thumb 函数，最低位必为 1。
- 空 Flash（0xFFFFFFFF）：最低位是 1，但前面 Flash 范围检查已经把它挡掉了。
- 损坏数据：如果最低位是 0，说明这不是合法的 Cortex-M 函数地址，跳过去会触发异常。

**所以 `& 1` 检查是一道保险，确认这个地址确实是 Cortex-M 能执行的函数。**

---

## 第六部分：软复位法（解决 A↔B 跳转卡死）

### 6.1 先理解"硬复位"和"软复位"

| 概念 | 触发方式 | 效果 |
|------|----------|------|
| **硬复位** | 按板子上的 RESET 键 / 上电 | 整个芯片从头开始，所有外设、寄存器、时钟恢复出厂默认 |
| **软复位** | 程序里调用 `NVIC_SystemReset()` | **效果和硬复位几乎一样**！外设全部复位，但不用手按按键 |

`NVIC_SystemReset()` 的本质是写一个特殊寄存器，让芯片自己复位自己。复位后从 0x08000000 重新开始执行（也就是 Bootloader）。

### 6.2 为什么软复位能解决花屏？

软复位 = 硬件复位 = **所有外设被强制恢复初始状态**：
- OLED 的 IIC 引脚 → 变回浮空输入（总线释放，不再死锁）
- TIM4/TIM2 → 停止计数
- USART2/USART3 → 关闭
- FreeRTOS → 彻底消失
- 所有时钟 → 恢复默认

复位后从 Bootloader 重新启动，环境**绝对干净**，就像刚上电一样。

### 6.3 用生活类比理解整个方案

**问题**：你想从 A 办公室换到 B 办公室，但 A 和 B 之间没有直达通道（直接跳转会卡死）。

**软复位法**：借助"大门口"（Bootloader）中转。

```
方案：留言板 + 重启

1. 你在门口留言板写张条子："我要去 B 办公室"
   （APP 把标志写进 RTC 备份寄存器）

2. 你按下"重启"按钮，回到大门口
   （APP 调用 NVIC_SystemReset，芯片复位，从 Bootloader 重新启动）

3. 大门口的保安（Bootloader）看留言板，发现你要去 B
   （Bootloader 读 RTC 备份寄存器的标志）

4. 保安直接带你从大门口去 B 办公室（这条路是通的，因为大门口环境干净）
   （Bootloader 跳转到 B 区 APP）

5. 保安顺手擦掉留言板，免得下次你路过又被误导
   （Bootloader 清除标志）
```

**关键**：A→B 直走不通，但"大门口→B"是通的（已验证可用）。所以借道大门口。

---

## 第七部分：RTC 备份寄存器是什么？

### 7.1 复位后什么东西会保留？

软复位后，绝大部分东西都被清零，但有几样**保留**：

| 复位后状态 | 内容 |
|-----------|------|
| **清零** | 外设寄存器、SRAM（部分）、时钟 |
| **保留** | RTC 备份寄存器（BKP0R~BKP19R） |

**RTC 备份寄存器**是 STM32 专门设计的一组"跨复位存储器"：
- 共 20 个，每个 32 位
- 由 VBAT 电池供电，断电也不丢
- 软复位/硬复位**都不会清除**它
- 专门用来在复位之间传递少量信息

### 7.2 为什么不用普通变量传标志？

你可能会问：为什么不直接用一个全局变量 `int jump_flag` 传标志？

因为：软复位后，C 启动代码会重新初始化全局变量（`.bss` 段清零，`.data` 段从 Flash 重载），**你写的标志会被清掉**！

而 RTC 备份寄存器独立于 SRAM，启动代码不碰它，软复位后值还在。**这就是用它传标志的原因。**

### 7.3 怎么读写备份寄存器？

```c
// 写之前必须先开"备份域访问权限"（默认是锁住的，防误写）
RCC_APB1PeriphClockCmd(RCC_APB1Periph_PWR, ENABLE);  // 开 PWR 时钟
PWR_BackupAccessCmd(ENABLE);                          // 解锁备份域

RTC->BKP0R = 0xA5A50002;   // 写标志到备份寄存器0

// 读不需要解锁
uint32_t flag = RTC->BKP0R;

// 用完要清零，否则下次复位还会读到这个标志
RTC->BKP0R = 0;
```

### 7.4 为什么用 0xA5A50002 这种怪数字？

这叫**魔数（Magic Number）**，用来防止误判。如果备份寄存器里是随机值或 0，不会误触发跳转。只有恰好等于 `0xA5A50001`（去A区）或 `0xA5A50002`（去B区）才有效。

- `0xA5A5` = 标志前缀（A5A5 是常用的魔数前缀，方便人眼识别）
- `0001` / `0002` = 目标分区编号

---

## 第八部分：两步流程完整图解

### 步骤一：APP 写标志 + 触发软复位

假设你现在在 **A 区**，长按 KEY4 想切到 **B 区**：

```c
// 在 A 区的 key_task.c 里
void KeyP4_LongPressedCallback(void)
{
    uint32_t target = PARTITION_B_ADDR;  // 目标：B区

    // ① 开备份域访问权限
    RCC_APB1PeriphClockCmd(RCC_APB1Periph_PWR, ENABLE);
    PWR_BackupAccessCmd(ENABLE);

    // ② 写标志："我要去 B 区"
    RTC->BKP0R = JUMP_FLAG_TO_B;   // = 0xA5A50002

    // ③ 打印一下，方便调试
    BLE_PRINTF("[KEY4] 触发软复位 -> 0x%08X\r\n", target);
    delay_ms(50);   // 等串口把字打完

    // ④ 触发软复位（这行之后，A区程序彻底结束，芯片重启）
    NVIC_SystemReset();   // 不会返回，下面的代码不会执行
}
```

**执行完这步后的状态**：
- RTC->BKP0R = 0xA5A50002（保留）
- 芯片复位，从 0x08000000（Bootloader）重新开始

### 步骤二：Bootloader 读标志 + 跳转

芯片复位后，从 Bootloader 的 main() 开始执行：

```c
// 在 Bootloader 的 main.c 里
int main(void)
{
    Func_LED_Init();   // 初始化 LED
    // LED 闪两下提示"我在Bootloader"
    ...
    delay_ms(100);

    // ① 开备份域访问权限
    RCC_APB1PeriphClockCmd(RCC_APB1Periph_PWR, ENABLE);
    PWR_BackupAccessCmd(ENABLE);

    // ② 读标志：APP 有没有留话？
    uint32_t jump_req = RTC->BKP0R;   // 读出来 = 0xA5A50002

    // ③ 立刻清标志，免得下次复位又跳
    RTC->BKP0R = 0;

    // ④ 根据标志跳转
    if (jump_req == JUMP_FLAG_TO_A && Check_APP_Valid(APP_A_ADDRESS)) {
        Jump_To_Application(APP_A_ADDRESS);   // 去 A 区
    }
    else if (jump_req == JUMP_FLAG_TO_B && Check_APP_Valid(APP_B_ADDRESS)) {
        Jump_To_Application(APP_B_ADDRESS);   // ← 这次走这里，去 B 区
    }

    // 如果没有跳转请求（正常上电），走默认逻辑
    if (Check_APP_Valid(APP_A_ADDRESS)) {
        Jump_To_Application(APP_A_ADDRESS);   // 默认去 A 区
    }
    ...
}
```

### 完整时序图

```
时间轴 ──────────────────────────────────────────────────────►

[A区运行中]  长按KEY4
   │
   │ ① 写 RTC->BKP0R = 0xA5A50002
   │ ② BLE_PRINTF 打印
   │ ③ NVIC_SystemReset()
   ▼
═══════════ 芯片硬件复位（外设全部清零，OLED总线释放）═══════════
   ▼
[Bootloader main()]
   │
   │ ① 读 RTC->BKP0R = 0xA5A50002
   │ ② 清 RTC->BKP0R = 0
   │ ③ Check_APP_Valid(0x080A0000) → 有效
   │ ④ Jump_To_Application(0x080A0000)
   ▼
[B区 SystemInit()]  VECT_TAB_OFFSET=0x000A0000 生效
   │
   ▼
[B区 main()]  正常运行，OLED不花屏 ✅
```

### 为什么这样就不花屏了？

对比"直接跳转"和"软复位法"：

```
直接跳转（会花屏）：
A区(IIC正在通信) ──关中断──► B区初始化OLED
                              │
                              └─ IIC总线还卡在A区通信中间状态
                                 → 起始条件失败 → 花屏

软复位法（不花屏）：
A区(IIC正在通信) ──软复位──► [硬件复位：IIC引脚变浮空，总线释放]
                              │
                              ▼
                          Bootloader(没动OLED)
                              │
                              ▼
                          B区初始化OLED
                              │
                              └─ IIC总线干净 → 正常初始化 ✅
```

**核心**：硬件复位这道"物理清零"是软件关外设无法完全替代的，所以软复位法最可靠。

---

## 第九部分：把所有概念串起来

回到 Bootloader 检查 APP 有效性的代码，现在每个词你都应该懂了：

```c
uint8_t Check_APP_Valid(uint32_t app_addr)
{
    volatile uint32_t *pAddr = (volatile uint32_t *)app_addr;
    uint32_t sp = pAddr[0];            // 【栈顶地址】APP启动时装进栈指针的值
    uint32_t reset_handler = pAddr[1]; // 【Reset_Handler】APP的启动函数入口

    int sp_valid = 0;
    // 检查【栈顶】是否在【SRAM1】范围
    if ((sp >= 0x20000000) && (sp < 0x20020000)) sp_valid = 1;
    // 检查【栈顶】是否在【SRAM2】范围
    else if ((sp >= 0x10000000) && (sp < 0x10010000)) sp_valid = 1;

    if (sp_valid) {
        // 检查【Reset_Handler】是否在 Flash 范围
        if ((reset_handler >= 0x08040000) && (reset_handler < 0x08100000)) {
            // 检查【Thumb位】（最低位=1）
            if (reset_handler & 1) return 1;  // APP有效！
        }
    }
    return 0;  // APP无效
}
```

| 代码 | 概念 | 一句话解释 |
|------|------|-----------|
| `sp = pAddr[0]` | 栈顶地址 | APP 启动时栈指针的初始值 |
| `0x20000000~0x2001FFFF` | SRAM1 | 主内存区，128KB |
| `0x10000000~0x1000FFFF` | SRAM2 | 高速RAM区，64KB |
| `reset_handler = pAddr[1]` | Reset_Handler | APP 的启动函数地址 |
| `reset_handler & 1` | Thumb 位 | 最低位=1 表示是 Thumb 指令函数 |
| `RTC->BKP0R` | RTC备份寄存器 | 软复位后不丢的"留言板" |
| `NVIC_SystemReset()` | 软复位 | 程序触发硬件复位 |

---

## 一句话总结

- **栈顶 (SP)**：APP 启动时栈指针的初始值，必须在真实 SRAM 里。
- **SRAM1/SRAM2**：芯片的两块内存，复杂 APP 的栈可能在 SRAM2，两个都要检查。
- **Reset_Handler**：APP 的真正入口函数（在 main 之前执行）。
- **Thumb 位**：函数地址最低位=1，表示是 Cortex-M 能执行的指令。
- **软复位**：程序里按"重启键"，外设全清零，环境变干净。
- **RTC 备份寄存器**：软复位后不丢的存储区，用来传递"我想跳哪个分区"的标志。
- **两步流程**：APP 写标志→软复位→Bootloader 读标志→跳目标分区。
